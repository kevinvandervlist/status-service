/// <reference path="../../src/typings/tsd.d.ts" />
import assert = require("power-assert");
import * as s from "../../src/services/service"

describe("services/observer_test.ts", () => {
    it("should foo()", (done) => {
        //var service = new s.Service("http://localhost:8080/demo/foo/health");
        //service.observe()
        //    .subscribe(
        //        n => {
        //            assert.equal(n.ServiceName, "Foo");
        //        },
        //        e => {
        //            done(e);
        //        },
        //        () => {
        //            done();
        //        }
        //    );
        done();
    });
});
